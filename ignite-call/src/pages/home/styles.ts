import { styled } from '@ignite-ui/react'

export const Container = styled('div', {
  maxWidth: 'calc(100vw - (100vw - 1160px))',
  marginLeft: 'auto',
  width: '1200',
  background: 'red',
  display: 'flex',
  alignItems: 'center',
  gap: '$20',
})

export const Hero = styled('div', {})

export const Preview = styled('div', {})
